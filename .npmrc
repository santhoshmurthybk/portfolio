# Use public npm registry
registry=https://registry.npmjs.org/

# Disable package-lock integrity check for cleaner regeneration
# package-lock=true

# Optional: Configure npm to save exact versions
# save-exact=true
